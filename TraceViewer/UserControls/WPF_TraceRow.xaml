<UserControl x:Class="TraceViewer.WPF_TraceRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TraceViewer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        
        <Style x:Key="StyledTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#353535"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                 Background="{TemplateBinding Background}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="Aqua"/>
                    <Setter Property="Background" Value="#001a1a"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="Aqua"/>
                    <Setter Property="Background" Value="#003333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StyledTextBlock" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <Style x:Key="TextBlockBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#252525"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="Aqua"/>
                    <Setter Property="Background" Value="#001a1a"/>
                </Trigger>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="Aqua"/>
                    <Setter Property="Background" Value="#001a1a"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Margin="0,0,0,0" >
         <DockPanel LastChildFill="False">
             <Border x:Name="id_border" Style="{StaticResource TextBlockBorderStyle}" Width="50" DockPanel.Dock="Left">
                <TextBlock x:Name="id" Width="50" Style="{StaticResource StyledTextBlock}" DockPanel.Dock="Left"/>
             </Border>
            <Border x:Name="address_border" Style="{StaticResource TextBlockBorderStyle}" Width="100" DockPanel.Dock="Left">
                <TextBlock x:Name="address" Width="100" Style="{StaticResource StyledTextBlock}" DockPanel.Dock="Left"/>
             </Border>
            <Border x:Name="disasm_border" Style="{StaticResource TextBlockBorderStyle}" Width="300" DockPanel.Dock="Left">
                <TextBlock x:Name="disasm" Width="300" Style="{StaticResource StyledTextBlock}" DockPanel.Dock="Left"/>
             </Border>
            <Border x:Name="changes_border" Style="{StaticResource TextBlockBorderStyle}" Width="300" DockPanel.Dock="Left">
                <TextBlock x:Name="changes" Width="300" Style="{StaticResource StyledTextBlock}" DockPanel.Dock="Left"/>
             </Border>
            <TextBox Style="{StaticResource StyledTextBox}" x:Name="comments" Width="300" DockPanel.Dock="Left"/>
         </DockPanel>
    </Grid>
</UserControl>