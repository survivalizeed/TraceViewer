<UserControl x:Class="TraceViewer.WPF_TraceRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TraceViewer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="StyledTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#353535"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                 Background="{TemplateBinding Background}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="Orange"/>
                    <Setter Property="Background" Value="#1a0500"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="Orange"/>
                    <Setter Property="Background" Value="#330a00"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StyledTextBlock" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <Style x:Key="TextBlockBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#353535"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="Orange"/>
                    <Setter Property="Background" Value="#1a0500"/>
                </Trigger>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="Orange"/>
                    <Setter Property="Background" Value="#1a0500"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <DockPanel LastChildFill="False">
        <Border Style="{StaticResource TextBlockBorderStyle}" Width="50">
            <TextBlock x:Name="id" Width="50" Style="{StaticResource StyledTextBlock}"/>
        </Border>
        <Border Style="{StaticResource TextBlockBorderStyle}" Width="200">
            <TextBlock x:Name="address" Width="200" Style="{StaticResource StyledTextBlock}"/>
        </Border>
        <Border Style="{StaticResource TextBlockBorderStyle}" Width="200">
            <TextBlock x:Name="disasm" Width="200" Style="{StaticResource StyledTextBlock}"/>
        </Border>
        <Border Style="{StaticResource TextBlockBorderStyle}" Width="200">
            <TextBlock x:Name="changes" Width="200" Style="{StaticResource StyledTextBlock}"/>
        </Border>
        <TextBox Style="{StaticResource StyledTextBox}" x:Name="comments" Width="300"/>
    </DockPanel>
</UserControl>