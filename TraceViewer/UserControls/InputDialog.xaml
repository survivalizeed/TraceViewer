<Window x:Class="TraceViewer.UserControls.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TraceViewer.UserControls"
        mc:Ignorable="d"
        Title="InputDialog" Height="100" Width="300"
        WindowStyle="None"
        ResizeMode="NoResize">
    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="Title" Grid.Row="0" Margin="0,0,0,10" Background="Transparent"/>
            <TextBox Style="{StaticResource comments_text_box}" Grid.Row="1"/>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK" IsDefault="True" Margin="0,0,10,0" Background="Transparent"/>
                <Button Content="Cancel" IsCancel="True" Background="Transparent"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
