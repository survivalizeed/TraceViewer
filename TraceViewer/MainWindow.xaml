<Window x:Class="TraceViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TraceViewer"
        mc:Ignorable="d"
        Title="MainWindow" 
        Background="#101010"
        Width="{x:Static SystemParameters.PrimaryScreenWidth}"  
        Height="{x:Static SystemParameters.PrimaryScreenHeight}"
        >
    <Window.Resources>
        <Style x:Key="titles" TargetType="Label">
            <Setter Property="DockPanel.Dock" Value="Left"/>
            <Setter Property="BorderBrush" Value="Orange"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid>
        <DockPanel Margin="0,104,0,0" LastChildFill="False" RenderTransformOrigin="0.501,-0.463" Height="41" VerticalAlignment="Top">
            <Label Style="{StaticResource titles}" Content="#" Width="50"/>
            <Label Style="{StaticResource titles}" Content="ADDRESS" Width="200"/>
            <Label Style="{StaticResource titles}" Content="DISASM" Width="200"/>
            <Label Style="{StaticResource titles}" Content="CHANGES" Width="200"/>
            <Label Style="{StaticResource titles}" Content="COMMENTS" Width="300"/>
        </DockPanel>

        <Grid Margin="0,150,450,0">
            <ItemsControl x:Name="instructions_view" VirtualizingStackPanel.IsVirtualizing="True"
              ScrollViewer.CanContentScroll="True">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.Template>
                    <ControlTemplate TargetType="ItemsControl">
                        <ScrollViewer CanContentScroll="True">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </ControlTemplate>
                </ItemsControl.Template>
            </ItemsControl>
        </Grid>
    </Grid>
</Window>
